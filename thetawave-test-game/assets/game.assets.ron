// Extended game assets - add your custom sprites and particle effects here
({
    // Extended/override sprites - these take precedence over base sprites
    // Key by file stem (e.g., "xhitara_spitter_mob" for xhitara_spitter_mob.aseprite)
    "extended_game_sprites": Files(
        paths: [
            // Add extended sprite paths here, e.g.:
            // "extended://media/aseprite/custom_mob.aseprite",
        ]
    ),
    // Extended particle effects - these take precedence over base effects
    // Key by file stem (e.g., "explosion" for explosion.ron)
    "extended_game_particle_effects": Files(
        paths: [
            // Add extended particle effect paths here, e.g.:
            // "extended://media/particles/custom_explosion.ron",
        ]
    ),
})
